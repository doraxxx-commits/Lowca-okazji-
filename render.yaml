services:
  - type: web
    name: football-simulation
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn football_engine:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
